{
  "name": "scrollmap-find-ar",
  "description": "Show find-and-replace results on the scrollbar",
  "repository": "https://github.com/asiloisad/pulsar-scrollmap-find-ar",
  "keywords": [
    "scrollmap",
    "find",
    "search",
    "marker"
  ],
  "version": "0.3.1",
  "license": "MIT",
  "engines": {
    "atom": ">=1.100.0 <2.0.0"
  },
  "main": "./lib/main",
  "consumedServices": {
    "find-and-replace": {
      "versions": {
        "0.0.1": "consumeFindService"
      }
    }
  },
  "providedServices": {
    "scrollmap": {
      "versions": {
        "1.0.0": "provideScrollmap"
      }
    }
  },
  "configSchema": {
    "threshold": {
      "order": 1,
      "type": "integer",
      "title": "Threshold",
      "description": "Hide markers if count exceeds this value. Zero means no limit.",
      "minimum": 0,
      "default": 0
    },
    "permanent": {
      "order": 2,
      "type": "boolean",
      "title": "Permanent view",
      "description": "Keep markers visible even when the find panel is closed.",
      "default": true
    }
  }
}
