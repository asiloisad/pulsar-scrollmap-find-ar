{
  "name": "scrollmap-find-ar",
  "description": "Find-and-replace result markers for scrollmap",
  "repository": "https://github.com/asiloisad/pulsar-scrollmap-find-ar",
  "keywords": [
    "scrollmap",
    "find",
    "search",
    "marker"
  ],
  "version": "0.2.0",
  "license": "MIT",
  "engines": {
    "atom": ">=1.100.0 <2.0.0"
  },
  "main": "./lib/main",
  "consumedServices": {
    "find-and-replace": {
      "versions": {
        "0.0.1": "consumeFindService"
      }
    }
  },
  "providedServices": {
    "scrollmap": {
      "versions": {
        "1.0.0": "provideScrollmap"
      }
    }
  },
  "configSchema": {
    "permanent": {
      "order": 1,
      "type": "boolean",
      "title": "Permanent view",
      "description": "Keep markers visible even when the find panel is closed.",
      "default": true
    },
    "threshold": {
      "order": 2,
      "type": "integer",
      "title": "Threshold",
      "description": "Hide markers if count exceeds this value. Zero means no limit.",
      "minimum": 0,
      "default": 0
    }
  }
}
